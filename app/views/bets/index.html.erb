<div class="container-fluid">
	<div class="row textarea-big">
		<h1>Visa svaren</h1>

		<table class="table table-hover table-responsive">
			<tbody>
				<% @bets.each do |b| %>
					<tr>
						<td><%= b.name %></td>
						<td><%= b.result %></td>
					</tr>
				<% end %>
			</tbody>
		</table>
	</div>
</div>
